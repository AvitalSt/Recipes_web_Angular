import { DurationFormatPipePipe } from './duration-format-pipe.pipe';

describe('DurationFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DurationFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
